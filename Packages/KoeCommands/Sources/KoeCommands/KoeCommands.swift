import Foundation

/// Main entry point for KoeCommands package
/// Provides voice command detection with speaker verification
public enum KoeCommands {
    /// Current version
    public static let version = "1.0.0"
}
